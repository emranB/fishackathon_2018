(function () {

    app.controller('home',
    ['$window', '$scope', '$routeParams', '$location', '$http', '$q', '$timeout', '$rootScope',
    function (
      $window,
      $scope,
      $routeParams,
      $location,
      $http,
      $q,
      $timeout,
      $rootScope
    ) {

        
    
    }]);
    
}());
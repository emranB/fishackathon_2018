(function () {

    app.controller('viewProfiles',
    ['$window', '$scope', '$routeParams', '$location', '$http', '$q', '$timeout', '$rootScope',
    function (
      $window,
      $scope,
      $routeParams,
      $location,
      $http,
      $q,
      $timeout,
      $rootScope
    ) {

        
    
    }]);
    
}());